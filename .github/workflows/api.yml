name: Teste and Deploy

on:
  push:
    branches: [main, dev]

  workflow_dispatch:
    inputs:
      environment:
        description: Environment
        type: choice
        required: true
        default: dev
        options:
              - web

jobs:
  detect-changes:
    runs-on: ubuntu-latest
    outputs:
      api: ${{ steps.changes.outputs.api }}
      app: ${{ steps.changes.outputs.app }}
    steps:
      - uses: actions/checkout@v6
      - uses: dorny/paths-filter@v4
        id: changes
        with:
          filters: |
            api:
              - 'api/**'
            app:
              - 'app/**'


  unitTestApi:
    needs: detect-changes
    if: needs.detect-changes.outputs.api == 'true'
    runs-on: ubuntu-latest
    permissions: write-all
    steps:
      - name: Get Code from repository
        uses: actions/checkout@v6
      - name: verifica se fez corretamente
        run:  ls -la
      - name: Instala o Node na maquina virtual        
        uses: actions/setup-node@v6
      - name: Instala as dependencias do projeto
        working-directory: api 
        run: npm ci
      - name: Roda os testes
        working-directory: api 
        run: npm run test 
      - name: verifica se fez corretamente
        working-directory: api 
        run:  ls -la        
      - name: Gera relatorio de testes
        uses: dorny/test-reporter@v3
        if: success() || failure()  # roda de qualquer maneira
        with:
          name: JEST tests
          path: ./api/j*.xml
          reporter: jest-junit
          
  deploy-api:
    needs: unitTestApi
    if: success()    
    runs-on: ubuntu-latest
    steps:
      - name: Deploy_Project_API
        run: |
          RESPONSE=$(curl --request POST \
               --url https://api.render.com/v1/services/${{ secrets.RENDER_SERVICE_ID_1 }}/deploys \
               --header 'accept: application/json' \
               --header 'authorization: Bearer ${{ secrets.RENDER_API_KEY }}' \
               --header 'content-type: application/json' \
               --data '{ "clearCache": "do_not_clear", "deployMode": "build_and_deploy" }')

          echo "Deploy response: $RESPONSE"

          DEPLOY_ID=$(echo $RESPONSE | jq -r '.id')
          echo "deploy_id=$DEPLOY_ID" >> $GITHUB_OUTPUT
          echo "Deploy ID: $DEPLOY_ID"

      - name: Wait for deploy to complete
        run: |
          DEPLOY_ID="${{ steps.Deploy_Project_API.outputs.deploy_id }}"
          SERVICE_ID="${{ secrets.RENDER_SERVICE_ID_1 }}"
          MAX_ATTEMPTS=30   # 30 x 20s = 10 minutos
          ATTEMPT=0

          echo "Aguardando deploy $DEPLOY_ID finalizar..."

          while [ $ATTEMPT -lt $MAX_ATTEMPTS ]; do
            STATUS=$(curl --silent \
              "https://api.render.com/v1/services/$SERVICE_ID/deploys/$DEPLOY_ID" \
              -H "Authorization: Bearer ${{ secrets.RENDER_API_KEY }}" \
              | jq -r '.status')

            echo "Tentativa $((ATTEMPT + 1)) — Status: $STATUS"

            if [ "$STATUS" = "live" ]; then
              echo "✅ Deploy concluído com sucesso!"
              exit 0
            elif [ "$STATUS" = "build_failed" ] || [ "$STATUS" = "deactivated" ] || [ "$STATUS" = "canceled" ]; then
              echo "❌ Deploy falhou com status: $STATUS"
              exit 1
            fi

            ATTEMPT=$((ATTEMPT + 1))
            sleep 20
          done

          echo "⏱️ Timeout: deploy não finalizou em 10 minutos"
          exit 1          